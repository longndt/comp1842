import { Payload } from "../interface/payload";
export declare class Jwt {
    private readonly secretKey;
    constructor();
    signAsync(payload: Payload, options: {
        expiresIn: string;
    }): Promise<unknown | string | undefined>;
}
