import { Payload } from "./interface/payload";
import { Jwt } from "./service/jwt";
/**
 * Extracts a token from the authorization header if it exists.
 *
 * @param {Object} headers - The headers object containing the authorization header.
 * @param {any} headers.authorization - The authorization header containing the token.
 * @return {string|null} The extracted token or null if the authorization header is not present.
 */
export declare function getTokenFromHeaders(headers: {
    [key: string]: any;
}): string | null;
export declare function generateAccessToken(payload: Payload, jwt: Jwt): Promise<unknown | string | undefined>;
export declare function getRandomNumberString(length: number): string;
export declare function paginate(page: number | undefined, limit: number | undefined, totalItems: number): {
    skip: number;
    take: number;
    totalPages: number;
    previousPage: number | null;
    nextPage: number | null;
};
